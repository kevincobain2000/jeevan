<!-- <div class="jarviswidget" id="wid-id-11">
  <header>
    <span class="widget-icon">
      <i class="fa fa-check txt-color-green"></i>
    </span>
    <h2>Education Mode</h2>
  </header>
  <div>
    <div class="widget-body no-padding"> -->

    <% categories = ['school', 'grad_college', 'grad', 'desired_country', 'post_grad', 'post_grad_college', 'highest_degree'] %>
      <dl class="dl-horizontal">
          <% categories.each do |category| %>
          <% heading = category.gsub "_", " " %>
          <dt><%= heading.capitalize %></dt>
          <dd><%= @user[:education][category] %></dd>
      <% end %>
      </dl>

    <!-- </div> -->
    <!-- end widget content -->
  <!-- </div> -->
  <!-- end widget div -->
<!-- </div> -->
