DELETE ME THIS PATH to remove ↑ blank white space
device/sessions/new.html.erb

<div id="sign_in1">
  <div class="container">
    <div class="row">
      <div class="span12 header">
        <h4>Log in to your account</h4>
        <p>
          There are many variations of passages of Lorem alteration in some form  injected humour these randomized words .</p>
      </div>
      <div class="span3 division">
        <div class="line l"></div>
        <span>↓</span>
        <div class="line r"></div>
      </div>
      <div class="span12 footer">
        <%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
          <%= f.text_field :email, :autofocus => true, :placeholder => "Email" %>
          <%= f.password_field :password, :placeholder => "Password" %>
          <% if devise_mapping.rememberable? -%>

          <% end -%>
          <%= f.submit "Sign in" %>
          <!-- <div class="remember">
          	<%= f.check_box :remember_me %> Remember Me
          </div> -->
        <% end %>
      </div>
      <div class="span12 proof">
        <div class="span5 remember">
          <%= render "devise/shared/links" %>
        </div>

      </div>
    </div>
  </div>
</div>
